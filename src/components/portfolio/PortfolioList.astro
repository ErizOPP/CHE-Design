---
import PortfolioCards from "../portfolio/PortfolioCards.astro";
import { getPortfolios } from '../../data/portfolio-wordpress.js';

const portoflios = getPortfolios()
---

<ul>
  {
    portoflios.res(({ id, title, img, description, category, href}) => {
      return (

          <PortfolioCards id={id} title={title} img={img} description={description} category={category} href={href}/>

      )
    })
  }
</ul>

<style>
  ul{
    width: fit-content;
    margin: auto;
  }
</style>